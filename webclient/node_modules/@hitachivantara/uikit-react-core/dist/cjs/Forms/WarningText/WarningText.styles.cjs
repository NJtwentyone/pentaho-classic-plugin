"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const uikitStyles = require("@hitachivantara/uikit-styles");
const classes = require("../../utils/classes.cjs");
const { staticClasses, useClasses } = classes.createClasses("HvWarningText", {
  root: { display: "none" },
  defaultIcon: { minWidth: "24px", width: "24px", height: "24px" },
  warningText: {
    color: uikitStyles.theme.colors.negative_120,
    paddingRight: uikitStyles.theme.space.xs
  },
  show: { display: "flex" },
  topGutter: { paddingTop: "3px" },
  hideText: {
    // display none or visibility hidden prevents
    // browser to trigger the aria-alert
    width: 0,
    height: 0,
    padding: 0,
    margin: 0,
    overflow: "hidden"
  },
  topBorder: { borderTop: `solid 1px ${uikitStyles.theme.colors.negative}` }
});
exports.staticClasses = staticClasses;
exports.useClasses = useClasses;
